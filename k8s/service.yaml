apiVersion: v1
kind: Service
metadata:
  name: springboot-cicd-poc-service
spec:
  selector:
    app: springboot-cicd-poc

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP